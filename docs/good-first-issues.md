# Good First Issues

1. Add a CSV input adapter for `npm run sync`.
2. Add a dashboard empty state when a synced sheet has no valid rows.
3. Add a schema preset for event attendance sheets with `member`, `event_count`, and `last_seen` columns.
